/* Minimal shim replacing header/processing/ASM/spectralmatrices.h
 * Provides only the constants that basic_parameters.c needs,
 * without pulling in RTEMS or other flight-software dependencies.
 */
#pragma once

#include "lfr_common_headers/fsw_params.h"
